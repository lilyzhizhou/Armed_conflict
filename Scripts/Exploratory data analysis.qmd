---
title: "Exploratory Data Analysis"
format: html
editor: visual
---

```{r, echo=FALSE}
source("../.Rprofile")
here()
data <- read.csv(here('Output/finaldata.csv'))

head(data)
```

```{r, echo = FALSE}
tail(data)

```

```{r, echo=FALSE}
data %>%
  summary()
```

-   sum has a large amount of NAs


```{r}
na_count <- colSums(is.na(data))

print(data.frame(Column = names(na_count), NA_Count = na_count))

```

- mat mortality has large number of NAs (426)
- all other mortality has 40 NAs

```{r}
table(data$earthquake)
table(data$drought)
```

## Ploting distribution of Continuous Data 

```{r}
 data %>%
  ggplot(aes(x = Year, y = sumdeath, group = ISO))+
  geom_line()+
  scale_y_log10()
```




